var express = require('express');
var router = express.Router();
var messageController = require('../controllers/messageController');
/*
router.route('/user/message/api')
  .get(messageController.listAPI)
  .post(messageController.createAPI);
router.route('/user/message/:id/api')
  .get(messageController.getAPI)
  .put(messageController.editAPI);
*/
